info:
  name: POST-Usuarios-login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/usuarios/login
  body:
    type: json
    data: |-
      {
        "username":"admin",
        "password":"adminpass"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
